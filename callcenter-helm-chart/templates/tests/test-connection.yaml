apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linhaverde-chart.fullname" . }}-test-connection"
  labels:
    {{- include "linhaverde-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linhaverde-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
